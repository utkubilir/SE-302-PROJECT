<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ProgressBar?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.shape.Circle?>
<?import javafx.scene.effect.DropShadow?>
<?import javafx.scene.effect.Glow?>

<StackPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" 
           styleClass="splash-screen" prefWidth="550" prefHeight="400">
    
    <!-- Background decoration circles -->
    <Circle radius="150" styleClass="splash-bg-circle-1" translateX="-200" translateY="-120" />
    <Circle radius="100" styleClass="splash-bg-circle-2" translateX="220" translateY="150" />
    <Circle radius="80" styleClass="splash-bg-circle-3" translateX="180" translateY="-100" />
    
    <!-- Glassmorphism card container -->
    <VBox alignment="CENTER" spacing="15" styleClass="splash-card" maxWidth="420" maxHeight="350">
        <padding>
            <Insets bottom="35" left="40" right="40" top="35" />
        </padding>
        
        <!-- App Icon with glow effect -->
        <StackPane>
            <ImageView fx:id="splashIcon" fitWidth="120" fitHeight="120" preserveRatio="true" styleClass="splash-icon">
                <image>
                    <Image url="@icon.png" />
                </image>
                <effect>
                    <DropShadow color="rgba(139, 92, 246, 0.5)" radius="20" spread="0.2" />
                </effect>
            </ImageView>
        </StackPane>
        
        <!-- App Title -->
        <Label text="Exam Planner" styleClass="splash-title" />
        
        <!-- Subtitle with icon -->
        <HBox alignment="CENTER" spacing="8">
            <Label text="📚" style="-fx-font-size: 14px;" />
            <Label text="Sınav Takvimi Yönetim Sistemi" styleClass="splash-subtitle" />
        </HBox>
        
        <!-- Spacer -->
        <Region prefHeight="10" />
        
        <!-- Loading Section -->
        <VBox alignment="CENTER" spacing="12" styleClass="splash-loading-section">
            <ProgressBar fx:id="progressBar" styleClass="splash-progress" prefWidth="320" />
            <HBox alignment="CENTER" spacing="8">
                <Label text="⏳" styleClass="splash-loading-icon" />
                <Label fx:id="lblStatus" text="Yükleniyor..." styleClass="splash-status" />
            </HBox>
        </VBox>
        
        <!-- Footer with version and copyright -->
        <Region prefHeight="5" />
        <HBox alignment="CENTER" spacing="20">
            <Label text="v1.0.0" styleClass="splash-version" />
            <Label text="•" styleClass="splash-version" />
            <Label text="© 2024 SE-302 Project" styleClass="splash-version" />
        </HBox>
    </VBox>
    
</StackPane>
